// Desativa e limpa todos os caches antigos do Service Worker para evitar tela branca em navegadores mobile
self.addEventListener('install', (event) => {
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((cacheNames) => {
      return Promise.all(
        cacheNames.map((cache) => caches.delete(cache))
      );
    }).then(() => self.clients.claim())
  );
});

// Nenhuma interceptacao de fetch estatico para evitar cache preso
